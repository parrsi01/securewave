# Installer Dependencies

This folder is populated by `windows_installer/build_windows_installer.ps1` during a Windows installer build.

Expected files:

- `wireguard-amd64.msi` (downloaded from `download.wireguard.com`)

`deps/` is gitignored.

