target-version = "py311"

[lint]
# Start with fatal syntax/name errors plus the safe unused-code baseline cleaned
# in the first refactor tranche. SQLAlchemy E711/E712 expressions are handled
# separately because generic Python boolean rewrites can change query semantics.
select = ["E9", "F401", "F541", "F63", "F7", "F811", "F82", "F841"]
