# Production Requirements
# Optimized for fast deployment - ML dependencies are OPTIONAL
# The VPN optimizer auto-detects ML availability and adapts accordingly

# FastAPI and ASGI server
fastapi==0.139.0
uvicorn[standard]==0.51.0
gunicorn==23.0.0
python-multipart==0.0.32

# Database
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.18.5

# Authentication & Security
PyJWT==2.13.0
passlib==1.7.4
bcrypt==3.2.2
cryptography==49.0.0
pyotp==2.10.0

# Payment Processing
stripe==10.12.0
httpx==0.28.1

# VPN & Networking
qrcode==7.4.2
pillow==12.3.0

# Environment & Configuration
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# Security & Rate Limiting
slowapi==0.1.10
redis==5.0.1

# Monitoring & Logging
sentry-sdk==2.65.0
psutil==7.2.2

# Email Services (Section 7)
sendgrid==6.12.5
boto3==1.43.49
jinja2==3.1.6

# DNS Operations (Section 8)
dnspython==2.8.0

# VPN Test Suite
PyYAML==6.0.3

# NOTE: ML packages (xgboost, numpy, scikit-learn) are OPTIONAL
# To enable ML-enhanced optimization, install requirements_ml.txt separately
# The app works perfectly without them using MARL-only mode
