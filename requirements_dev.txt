# Development Requirements (includes everything + ML packages)
# Use this for local development if you want full ML capabilities

-r requirements.txt
-r requirements_ml.txt

# Testing
pytest==9.0.3
pytest-cov==6.0.0
pytest-asyncio==1.4.0
httpx==0.27.0
ruff==0.15.21
